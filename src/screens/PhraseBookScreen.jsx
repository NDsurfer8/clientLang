import { View, Text, StyleSheet } from 'react-native'
import React from 'react'

const PhraseBookScreen = () => {
    return (
        <View>
            <Text>PhraseBookScreen</Text>
        </View>
    )
}

export default PhraseBookScreen
const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
});